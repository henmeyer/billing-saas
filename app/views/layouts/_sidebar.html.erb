<nav class="w-56 bg-gray-900 flex flex-col flex-shrink-0">
  <div class="h-14 flex items-center px-4 border-b border-gray-700">
    <span class="text-white font-semibold text-sm tracking-wide">
      💳 Billing
    </span>
  </div>

  <div class="flex-1 py-4 overflow-y-auto">
    <div class="space-y-0.5 px-2">
      <%= render 'layouts/nav_link',
            path: root_path,
            icon: '◻',
            label: 'Dashboard' %>

      <div class="pt-4 pb-1 px-2">
        <p class="text-xs font-medium text-gray-500 uppercase tracking-wider">Gestão</p>
      </div>

      <%= render 'layouts/nav_link',
            path: plans_path,
            icon: '▤',
            label: 'Planos' %>

      <%= render 'layouts/nav_link',
            path: customers_path,
            icon: '◉',
            label: 'Clientes' %>

      <div class="pt-4 pb-1 px-2">
        <p class="text-xs font-medium text-gray-500 uppercase tracking-wider">Configurações</p>
      </div>

      <%= render 'layouts/nav_link',
            path: integrations_path,
            icon: '⇄',
            label: 'Integrações' %>

      <%= render 'layouts/nav_link',
            path: api_keys_path,
            icon: '⚷',
            label: 'API Keys' %>

      <%= render 'layouts/nav_link',
            path: payment_gateways_path,
            icon: '◈',
            label: 'Gateways' %>
    </div>
  </div>

  <div class="border-t border-gray-700 px-4 py-3">
    <div class="flex items-center gap-3">
      <div class="w-7 h-7 rounded-full bg-brand-600 flex items-center justify-center text-white text-xs font-medium flex-shrink-0">
        <%= current_user.name.first.upcase %>
      </div>
      <div class="flex-1 min-w-0">
        <p class="text-xs font-medium text-white truncate"><%= current_user.name %></p>
        <p class="text-xs text-gray-400 truncate"><%= current_user.account.name %></p>
      </div>
    </div>
    <div class="mt-2">
      <%= link_to "Sair", destroy_user_session_path, data: { turbo_method: :delete },
            class: "text-xs text-gray-400 hover:text-white transition-colors" %>
    </div>
  </div>
</nav>
